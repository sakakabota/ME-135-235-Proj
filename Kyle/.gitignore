__pycache__/
*.pyc
*.pyo
.venv/
.env

# Local docs DB (binary, not useful in git)
docs/history.db
docs/PENDING_IMPROVEMENTS.json

# Model weights — fetched on first run, too big for git
*.pt

# PlatformIO build artifacts
firmware/*/.pio/
firmware/*/.vscode/

# orchestrator.py scratch output (legacy generator; archived results live in _archive/agent_outputs/)
agent_outputs/
